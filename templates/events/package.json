{
  "name": "{{PKG_NAME}}",
  "version": "0.1.0",
  "description": "{{PKG_DESCRIPTION}}",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "README.md",
    "cordis.patch.yml",
    "dist"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "^{{CORDIS_VERSION}}",
    "@deepseek-ai/dsh-session": "^{{DSH_SESSION_VERSION}}",
    "@deepseek-ai/dsh-tools": "^{{DSH_TOOLS_VERSION}}",
    "@deepseek-ai/schemastery": "^{{SCHEMASTERY_VERSION}}",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
